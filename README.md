# Ricardo Lima's NeoVim Configuration

This repository contains all my configuration for NeoVim, which should work on **Linux**.


# Install

git clone repository for ***~/.config/nvim***

and

open ```nvim``` execute ```:PlugInstall```

